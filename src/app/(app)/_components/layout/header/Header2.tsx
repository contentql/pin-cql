interface Props {
  scroll?: boolean
  isMobileMenu?: boolean
  handleMobileMenu?: Function
  headerCls?: string
}

export default function Header2({
  scroll,
  isMobileMenu,
  handleMobileMenu,
  headerCls,
}: Props) {
  return <>Header2</>
}
